# Agents 文档入口

本目录用于给 Codex 提供“最小但可靠”的上下文来源。阅读顺序建议如下：

1. `AGENTS.md`：当前任务与上下文（短、临时）。
2. `skills/`：长期稳定的行为边界与规范。
3. `reference/`：后端事实来源（API 行为 / 状态机 / 资源说明）。
4. `types/`：类型速查（极省 token）。
5. `patterns/`：固定写法模板（强烈推荐）。
6. `decisions/`：架构决策记录（ADR，可选）。

如果 `AGENTS.md` 与其他文档冲突，以 `AGENTS.md` 为准。
